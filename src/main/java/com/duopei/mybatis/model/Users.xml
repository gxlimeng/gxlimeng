<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.duopei.mybatis.model.UserMapper">
	<select id="selectUserByID" parameterType="int" resultType="com.duopei.mybatis.model.Users">
		select sno id,name userName from tuser where sno = #{id}
	</select>
</mapper>